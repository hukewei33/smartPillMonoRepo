{
  "name": "smartpill-server",
  "version": "0.1.0",
  "description": "Smart medication tracking API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "JWT_SECRET=test-secret node --test test/"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  },
  "dependencies": {
    "argon2": "^0.41.0",
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.4.0",
    "express": "^4.21.0",
    "express-jwt": "^8.0.0",
    "jsonwebtoken": "^9.0.0"
  }
}
