# Required for auth (register, login, protected routes). Use a long random string in production.
JWT_SECRET=your-secret-at-least-32-chars

# Optional. Default 3000.
PORT=3000

# Optional. Default: ./data/smartpill.db
# DB_PATH=./data/smartpill.db
